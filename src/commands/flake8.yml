description: >
  Run flake8 command on specified src.
parameters:
  src:
    default: src
    description: Path to the directory containing the python files.
    type: string
steps:
  - run: flake8 << parameters.src >>
